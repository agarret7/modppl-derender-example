# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# generated training data / weights (regenerate via gen_dataset / train)
out/dataset/
*.safetensors

# test-run output: `cargo test -p modppl-derender` runs with the crate dir as
# cwd, so its "./out/..." paths land here, separate from the tracked
# top-level out/ gallery assets
derender/out/

# ad hoc live-demo snapshots and stray backups
out/snapshot_*.bmp
out.bak/

# warp_into_quad.sh intermediate per-frame scratch (mask + hundreds of
# rasterized trace-text frames); only its top-level composited/fused outputs
# are real deliverables
out/composited/mask.png
out/composited/trace_frames/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/